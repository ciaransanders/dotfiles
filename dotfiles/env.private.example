# Machine-local secrets. Copy to env.private and fill in.
# env.private is gitignored via the *.private rule and must never be committed.

export ANTHROPIC_API_KEY=""
export CLAUDE_CODE_PRO_SUBSCRIPTION_AUTH_TOKEN=""
